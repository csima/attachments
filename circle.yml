machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t csima/attachments_worker attachments_worker/

deployment:
  hub:
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push csima/attachments_worker
